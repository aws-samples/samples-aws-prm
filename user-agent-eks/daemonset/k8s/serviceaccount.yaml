# The DaemonSet's own service account. daemonset-stack.yaml binds this
# (namespace + name) to a dedicated IAM role via an EKS Pod Identity association.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: prm-daemonset-sa
  namespace: prm-daemonset
