# The sidecar's own service account. The sidecar-stack.yaml CloudFormation
# template binds this (namespace + name) to a dedicated IAM role via an EKS Pod
# Identity association, so the sidecar gets temporary credentials automatically.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: prm-sidecar-sa
  namespace: prm-sidecar
