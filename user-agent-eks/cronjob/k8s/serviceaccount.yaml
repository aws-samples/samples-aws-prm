# ServiceAccount that the CronJob pods run as. EKS Pod Identity associates this
# ServiceAccount with the IAM role (handled by the deploy script via
# `aws eks create-pod-identity-association`), so pods receive temporary
# credentials automatically — no static keys.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: describe-instance-sa
  namespace: prm-demo
